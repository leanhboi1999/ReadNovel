<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ReadNovel" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="android:colorAccent">@color/midColor</item>
        <item name="android:colorPrimaryDark">@color/centerColor</item>
        <item name="android:colorPrimary">@color/yellow_500</item>
        <!-- Customize your theme here. -->
    </style>
</resources>